+++
title = "The Filesystem as State"
date = "2026-03-10"
description = "Paths, permissions, inodes, /proc, /sys. The agent's working memory is the filesystem."
tags = ["filesystem", "linux", "permissions", "proc", "bootcamp"]
step = 3
tier = 1
estimate = "4-6 hours"
+++

Step 3 of 12 in the Agentic Engineering Bootcamp.

---

Paths, permissions, inodes, /proc, /sys. The agent's working memory is the filesystem.

## Key Topics

- Inodes, hard links, and symlinks - what a file actually is
- Path resolution and the difference between absolute and relative paths
- Permissions, ownership, and umask
- /proc as a window into process state
- /sys as a window into kernel and hardware state
- Temporary files, atomic writes, and race conditions
- Filesystem as the agent's working memory - reading and writing state

---

Full content in development. [Step 1: The Unix Process Model](/bootcamp/01-process-model/) is available as a sample.
